import os,sys,time,argparse,h5py,glob,cv2;import numpy as np;import torch,torch.nn as nn,torch.optim as optim;from torch.utils.data import Dataset,DataLoader;import torchvision.models as models;from torchvision.models import ResNet18_Weights;sys.path.insert(0,os.path.dirname(os.path.dirname(os.path.abspath(__file__))));NUM_FRAMES=4;ACTION_MAP={0:0,4:1,6:2,5:3,3:4};ACTION_NAMES=['idle','forward','turn_left','turn_right','dodge'];NUM_CLASSES=5;exec(open(os.path.join(os.path.dirname(os.path.abspath(__file__)),'goal_conditioned_bc_v51_impl.py')).read())