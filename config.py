"""
wukong_ai - 配置中心
所有超参数和游戏窗口参数集中管理，一键调参
"""
import os

# ============================================================
# 游戏窗口配置
# ============================================================
GAME_WINDOW_TITLE = "黑神话：悟空"  # 游戏窗口标题（用于定位）

# 游戏窗口区域（全屏时通常等于屏幕分辨率，需根据实际调整）
# 游戏窗口区域（自动检测或手动设置）
# 窗口化模式示例: left=249, top=90, width=1038, height=614
# 全屏模式: left=0, top=0, width=1920, height=1080
GAME_REGION = {
    "top": 90,
    "left": 249,
    "width": 1038,
    "height": 614,
}

# 血量检测区域（相对于游戏窗口左上角的坐标）
BLOOD_REGION = {
    # 天命人（玩家）血条
    "player_hp": {
        "top": 1010,    # 血条顶部 y
        "left": 270,    # 血条左侧 x
        "width": 230,   # 血条宽度
        "height": 12,   # 血条高度
    },
    # 虎先锋（Boss）血条 - 显示在屏幕上方
    "boss_hp": {
        "top": 40,
        "left": 500,
        "width": 920,
        "height": 18,
    },
    # 气力条（体力）
    "player_stamina": {
        "top": 1028,
        "left": 270,
        "width": 190,
        "height": 8,
    },
}

# 血量HSV颜色范围（黑神话悟空的血条是红色/橙色）
HSV_RANGES = {
    "player_hp": {
        "lower": [0, 100, 100],      # 红色下界
        "upper": [10, 255, 255],     # 红色上界
        "lower2": [170, 100, 100],   # 红色第二区间下界（HSV红色跨越0度）
        "upper2": [180, 255, 255],   # 红色第二区间上界
    },
    "boss_hp": {
        "lower": [0, 80, 80],
        "upper": [15, 255, 255],
        "lower2": [165, 80, 80],
        "upper2": [180, 255, 255],
    },
    "player_stamina": {
        "lower": [20, 100, 100],     # 黄色/橙色
        "upper": [35, 255, 255],
    },
}

# ============================================================
# 截图配置
# ============================================================
CAPTURE_BACKEND = "dxcam"  # dxcam | mss | win32
CAPTURE_FPS = 60           # 目标帧率
FRAME_STACK = 4            # 帧堆叠数量（给模型时序信息）
FRAME_WIDTH = 224          # 模型输入宽度
FRAME_HEIGHT = 224         # 模型输入高度

# ============================================================
# 动作空间配置
# ============================================================
# 动作定义：每个动作是一个按键组合
# 格式: (name, keys, duration_ms)
ACTION_SPACE = [
    ("idle",         [],                           0),      # 0: 不操作
    ("attack",       ["j"],                        100),    # 1: 轻击（J=鼠标左键映射）
    ("heavy_attack", ["j", "j", "j", "j"],         [80, 80, 80, 400]),  # 2: 重击（4连轻击触发）
    ("dodge",        ["space"],                    150),    # 3: 闪避
    ("move_forward", ["w"],                         200),    # 4: 前进
    ("move_right",   ["d"],                         200),    # 5: 右移
    ("move_left",    ["a"],                         200),    # 6: 左移
    ("dodge_attack", ["space", "j"],               [150, 100]),  # 7: 闪避+攻击
    ("lock_on",      ["v"],                         100),    # 8: 锁定
    ("heal",         ["r"],                         200),    # 9: 使用葫芦回血
]

NUM_ACTIONS = len(ACTION_SPACE)

# 按键映射（directkeys格式）
KEY_MAP = {
    "w": 0x11,     # W
    "a": 0x1E,     # A
    "s": 0x1F,     # S
    "d": 0x20,     # D
    "space": 0x39, # Space
    "j": 0x24,     # J (映射到鼠标左键点击)
    "k": 0x25,     # K (映射到鼠标右键/重击)
    "v": 0x2F,     # V (锁定)
    "r": 0x13,     # R (葫芦)
    "shift": 0x2A, # Shift (奔跑)
}

# ============================================================
# 模型配置
# ============================================================
MODEL = {
    "encoder": "resnet18",     # resnet18 | cnn | vit
    "pretrained": True,        # 使用ImageNet预训练
    "freeze_encoder": False,   # 是否冻结编码器（初期可冻结加速训练）
    "latent_dim": 256,         # 编码器输出维度
    "blood_embed_dim": 32,     # 血量信息嵌入维度
    "hidden_dim": 512,         # MLP隐藏层维度
}

# ============================================================
# PPO配置
# ============================================================
PPO = {
    "lr": 3e-4,               # 学习率
    "gamma": 0.99,             # 折扣因子
    "gae_lambda": 0.95,        # GAE lambda
    "clip_epsilon": 0.2,       # PPO裁剪参数
    "entropy_coef": 0.01,      # 熵正则化系数
    "value_coef": 0.5,         # 价值函数损失系数
    "max_grad_norm": 0.5,      # 梯度裁剪
    "ppo_epochs": 4,           # 每次更新的PPO epoch数
    "minibatch_size": 64,      # mini-batch大小
    "rollout_length": 2048,    # 每次rollout的步数
}

# ============================================================
# 经验回放配置
# ============================================================
REPLAY = {
    "buffer_size": 100000,     # 经验回放池大小
    "prioritized": True,       # 使用优先经验回放
    "alpha": 0.6,              # 优先级指数
    "beta_start": 0.4,         # 重要性采样初始beta
    "beta_frames": 100000,     # beta增长到1.0的帧数
}

# ============================================================
# 训练配置
# ============================================================
TRAIN = {
    "total_frames": 10_000_000,   # 总训练帧数
    "save_interval": 50000,       # 每N步保存模型
    "log_interval": 1000,         # 每N步记录日志
    "eval_interval": 10000,       # 每N步评估
    "save_dir": "checkpoints",    # 模型保存目录
    "log_dir": "logs",            # 日志目录
    "device": "cuda",             # cuda | cpu
    "seed": 42,
}

# ============================================================
# 行为克隆配置（寻路阶段）
# ============================================================
BC = {
    "lr": 1e-3,
    "epochs": 50,
    "batch_size": 16,
    "data_dir": "pathfinding_data",  # 录制的demo数据目录
    "augment": True,                  # 数据增强
}

# ============================================================
# 死亡/胜利检测
# ============================================================
DETECT = {
    "death_threshold": 0.02,      # 血量低于此值判定死亡
    "victory_wait": 5.0,          # 击杀boss后等待秒数
    "death_wait": 3.0,            # 死亡后等待秒数
    "restart_keys": ["space"],    # 重新开始按键
    "restart_wait": 8.0,          # 重开等待加载时间
}

# ============================================================
# 工具函数
# ============================================================
def get_save_dir():
    """获取模型保存目录"""
    d = os.path.join(os.path.dirname(__file__), TRAIN["save_dir"])
    os.makedirs(d, exist_ok=True)
    return d

def get_log_dir():
    """获取日志目录"""
    d = os.path.join(os.path.dirname(__file__), TRAIN["log_dir"])
    os.makedirs(d, exist_ok=True)
    return d
